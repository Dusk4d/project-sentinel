# Changelog

本项目遵循语义化版本。日期使用北京时间对应的自然日。

## Unreleased

### Added

- 构建生成可复现 CycloneDX 1.6 JSON SBOM，并随 ZIP、CI 制品和正式 Release 分发。
- 标签发布为 ZIP、校验文件和 SBOM 生成可验证的 SLSA 来源证明，并把 CycloneDX SBOM 作为 ZIP 的签名证明关联到 GitHub。

## 0.3.0 - 2026-09-09

### Added

- 随发行包提供上线检查、监控、故障定位、状态备份、升级和回滚运行手册。
- 标准化 Function Calling 工具目录与结构化调用结果，五个只读工具共享现有工作区安全边界。
- 纯本地 RAG：有界文本分块、中英文词元、BM25 风格检索、相对路径与行号证据，以及文本/JSON 输出。
- Web 看板内置本地 RAG 项目问答，提供受限 API、证据路径与行号展示。
- 可选 OpenAI Chat Completions 兼容模型增强 RAG，支持本机 HTTP、远程 HTTPS、超时/响应边界和离线降级。
- Web 看板可显式选择模型增强 RAG，并公开无敏感配置的能力状态；未配置和模型故障均有确定性处理。
- 完整 Function Calling Agent 循环：模型选工具、封闭参数校验、只读执行、工具结果回传和轮次/调用总量限制。
- Web 看板 Agent 入口与 `/api/agent-ai`，让浏览器端也能执行同一套受限 Function Calling 工具循环。
- Function Calling 回答附带有界工具审计轨迹，公开调用顺序、工具名和成功状态而不重复泄露工具输出。
- 模型请求增加 1 MiB UTF-8 字节硬上限，阻止多轮工具结果累积成无界外发请求。
- Web 服务校验浏览器 `Origin`，只接受与当前本机端口同源的回环来源，阻断跨站页面触发扫描或模型调用。
- 可选持久 Agent 记忆：工作区身份绑定、原子更新、并发锁、损坏检测、容量淘汰和有界历史上下文。
- 独立 ZIP 发行包、免 Maven 跨平台启动器、CI 分发烟雾测试与 SHA-256 制品，以及版本匹配的标签发布工作流。
- Maven 项目版本经资源过滤成为运行时唯一版本来源，并为 JAR 与 ZIP 固定构建时间戳以支持可复现构建。
- 稳定规则 ID、规则目录、选择性禁用及带负责人和到期日的风险豁免。
- 可配置评分权重、配置初始化与有效配置校验；未知键和未知规则 ID 会直接失败。
- 离线 HTML 看板、稳定 JSON 健康报告、机器可读行动计划和原子完成清单。
- 仅监听本机的 Java Web 后端、工作区项目选择、动态健康看板及健康检查和实时扫描 API。
- 单次扫描生成健康报告与优先行动计划的组合 API，Web 看板展示预计恢复分和排序依据。
- 多项目发现与组合巡检，并支持工作区配置继承和最低健康分门禁。
- 显式构建验证、超时和输出边界、构建证据历史及完整日志归档。
- 每日健康回归门禁、状态目录跨进程锁、运行所有者和状态查询。
- 独立的版本化高风险基线，检测被净分变化掩盖的新增 HIGH 风险。
- 主流 CI 配置发现，以及带相对路径和行号的有界 TODO 证据。
- 本地 Web 看板支持上传 ZIP 项目并返回健康报告与行动计划，包含大小、条目数和路径穿越防护。
- Web 看板可将当前健康报告和行动计划下载为格式化 JSON 文件。

### Changed

- CLI 标准输出与错误输出在入口统一使用 UTF-8，避免 Windows PowerShell 重定向或宿主捕获时出现中文乱码。
- 产品展示名确定为 Project Sentinel（项目哨兵），并增加 Windows 一键 Web 启动器。
- 源码与测试识别扩展到 JSX/TSX、Vue、Svelte、.NET、Ruby、PHP、Swift、Dart、脚本语言等常见类型。
- 项目发现增加明确数量上限和截断状态，防止大型工作区产生无界内存增长或不完整静默门禁。
- Web 扫描增加单实例准入控制，并发请求返回可重试的 HTTP 429，健康检查保持可用。
- Web 服务拒绝非回环 Host，降低浏览器 DNS rebinding 访问本地分析接口的风险。
- 报告归档名称加入毫秒时间戳和随机后缀，快速连续运行不再碰撞。
- 历史快照追加前校验表头、项目、取值范围和时间单调性。
- 忽略目录在遍历阶段直接剪枝；达到文件上限后仅额外探测一个文件并立即停止。

### Fixed

- 文本搜索不再跟随指向工作区外部文件的符号链接，Function Calling 与交互命令共享此修复。
- Windows 启动器每次启动都执行增量打包，避免已有 JAR 掩盖更新后的源码。
- 恢复 Unix Maven Wrapper 可执行位并升级 GitHub Actions 运行时，修复 Linux CI 的退出码 126。
- 构建命令检测测试显式覆盖 Windows 和 Unix，消除依赖测试宿主系统的错误断言。

- 严格拒绝 CLI 多余参数，避免用户输入被静默忽略。
- 文件数恰好等于扫描上限时不再误报结果截断。
- Windows CLI 不再强制 UTF-8 覆盖终端原生编码，修复代码页 936 下的中文乱码。
- README 检测不再被嵌套文件、相似前缀文件或空白占位文件错误满足。
- 空白构建清单不再被计为可用构建能力，并使用独立高风险规则报告。

## 0.2.0 - 2026-09-08

- 增加项目健康分析、风险分级和行动计划。
- 增加 Markdown、JSON、项目组合报告和历史趋势。
- 增加无人值守每日运行及稳定退出码。
- 增加项目级扫描配置和依赖锁定检查。
- 增加真实路径、符号链接与目录联接越界防护。
- 采用 JUnit 5、Maven Wrapper 和可执行 JAR。

## 0.1.0 - 2026-09-07

- 建立 Java 21 CLI、工作区边界和只读文件工具。
